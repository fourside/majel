apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: majel

resources:
  - namespace.yaml
  - app-deployment.yaml
  - app-service.yaml
  - configmap.yaml

generators:
  - secret-generator.yaml
